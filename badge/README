Make a badge thus:
  $ cat ../example_print_badge_input | ./print_badge -n

Configure a badge thus:
  $ emacs configuration/print_badge.config.json

Binaries for GD are in the osx/ directory

Install GD on debian with "apt-get install python-gd"
